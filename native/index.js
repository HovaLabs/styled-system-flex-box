import styled from 'styled-components/native';

import { createAbsolute, createAbsoluteFlex, createBox, createFlex } from '../components';

export const Absolute = createAbsolute(styled.View);
export const AbsoluteFlex = createAbsoluteFlex(style.View);
export const Box = createBox(styled.View);
export const Flex = createFlex(styled.View);
